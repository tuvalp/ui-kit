import "./Divider.css";
interface DividerProps {
    vertical?: boolean;
    dashed?: boolean;
    color?: string;
    thickness?: string;
    length?: string;
    spacing?: string;
}
export declare function Divider({ vertical, dashed, color, thickness, length, spacing, }: DividerProps): import("react/jsx-runtime").JSX.Element;
export {};
